apiVersion: v1
kind: Service
metadata:
  name: pgadmin-service
  namespace: exp
spec:
  type: NodePort
  selector:
    app: pgadmin
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30502  # Exposes pgAdmin on http://localhost:30502
